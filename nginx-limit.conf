# Rate limit product pages: 10 req/s per IP, burst 20
limit_req_zone $binary_remote_addr zone=product_limit:10m rate=10r/s;

location ~ /product/ {
    limit_req zone=product_limit burst=20 nodelay;
    try_files $uri $uri/ /index.php?$args;
}
